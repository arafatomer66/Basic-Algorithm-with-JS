

const nameStr : string  =  "aRafat" ;
const lower : string = nameStr.toLowerCase();
const first : string = lower.charAt(0) ; 
const upper : string = first.toUpperCase();
const remain : string = lower.slice(1);

function titleCase(str){
  return str.charAt(0).toUpperCase() + lower 
}
